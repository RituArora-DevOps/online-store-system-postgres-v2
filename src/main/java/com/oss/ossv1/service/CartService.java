package com.oss.ossv1.service;

public class CartService {
    // Since our shopping cart is not persisted in the database,
    // it behaves more like a temporary, in-memory object tied to the current session or application state.

}
